# Jarvis Voice TUI — Python dependencies
# Install: pip install -r requirements.txt

# Speech-to-Text — local Whisper (no API key, runs on-device)
# Install torch first from the bundled wheels if on ARM:
#   pip install torch.whl torchaudio.whl
#   pip install faster-whisper
faster-whisper>=1.0.0

# Voice Activity Detection (VAD) - local, no API key needed
webrtcvad>=2.0.10

# HTTP client (for OpenClaw gateway API calls)
requests>=2.31.0
