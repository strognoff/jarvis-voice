# Jarvis Voice TUI — Python dependencies
# Install: pip install -r requirements.txt

# Voice Activity Detection (VAD) - local, no API key needed
# We patch webrtcvad to remove pkg_resources dependency (fixed automatically on install)
webrtcvad>=2.0.10

# OpenClaw gateway calls use Python's standard library HTTP client.

# ──────────────────────────────────────────────────────────────────────
# Optional / advanced VAD (requires torch — compatibility varies):
#   pip install torch torchaudio silero-vad
# silero-vad gives better quality than WebRTC VAD but needs PyTorch
# ──────────────────────────────────────────────────────────────────────
